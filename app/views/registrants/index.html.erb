<h1><%= pluralize(@registrants.size, 'Registrant') %></h1>

<ul>
  <% @registrants.each do |registrant| %>
    <li><%= registrant.first_name %> 
    <%= registrant.last_name %></li>
  <% end %> 
</ul>